/**
 * Description: Nearly every standard library data structure supports the size() method, 
 * which returns the number of elements in the data structure, 
 * and the empty() method, which returns true if the data structure is empty, 
 * and false otherwise.
 */